#textdomain wesnoth-test
[binary_path]
    path=data/test/images/PngSuite-2017jul19
[/binary_path]
[test]
    name=_"Image loading test"
    map_file=test/maps/generic_unit_test.map
    turns=unlimited
    id=image_loading_test
    # TODO: actually write the code here
    # idea is to ensure that all the .png files from pngsuite will load
    # (or fail to load) properly when Wesnoth attempts to use them
    # Something like (pseudocode):
    # for x in 1 to (max x)
    #   for y in 1 to (max y)
    #     do place image at x,y for next image in the pngsuite
[/test]

# vim: tabstop=4: shiftwidth=4: expandtab: softtabstop=4: autoindent:
# kate: indent-mode normal; space-indent on;
